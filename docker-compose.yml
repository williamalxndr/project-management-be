services:
  backend:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    command: npm run dev
    volumes:
      - ./src:/app/src
      - ./tsconfig.json:/app/tsconfig.json
    restart: unless-stopped
